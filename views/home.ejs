<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Nimtt Student Support System">
    <title>Nimtt Student Support</title>
    <link rel="shortcut icon"
        href="https://ik.imagekit.io/7de5cntkw/1692626704665-photoaidcom-cropped%20(1).jpeg?updatedAt=1738048156918"
        type="image/x-icon">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #1e3a8a, #f97316);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            color: white;
            margin: 0;
        }

        .header {
            background: rgba(255, 255, 255, 0.1);
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
            padding: 1rem 1.5rem;
        }

        .footer {
            background: rgba(255, 255, 255, 0.1);
            border-top: 1px solid rgba(255, 255, 255, 0.2);
            padding: 1.5rem;
            text-align: center;
        }

        .custom-button {
            background:#3b82f6;
            color: white;
            font-weight: 600;
            text-transform: uppercase;
            border-radius: 9999px;
            font-size: 1rem;
            width: 200px;
            height: 54px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: none;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
            transition: all 0.3s ease;
            gap: 0.75rem;
            position: relative;
            overflow: hidden;
        }

        .custom-button:hover {
            background:#f97316;
            transform: scale(1.03);
            box-shadow: 0 4px 12px rgba(249, 115, 22, 0.3);
        }

        .custom-button:focus {
            outline: none;
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.4);
        }

        .custom-button i {
            font-size: 1.2rem;
        }

        .custom-button::after {
            content: '';
            position: absolute;
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            transform: translate(-50%, -50%);
            transition: width 0.5s ease, height 0.5s ease;
            pointer-events: none;
        }

        .custom-button:active::after {
            width: 200px;
            height: 200px;
        }

        .button-grid {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 1.5rem;
            max-width: 440px;
            margin: 0 auto;
        }

        @media (min-width: 768px) {
            .button-grid {
                flex-wrap: nowrap;
            }
        }

        @media (max-width: 640px) {
            .header img {
                width: 32px;
                height: 32px;
            }

            h1.text-4xl {
                font-size: 2.25rem;
            }

            p.text-lg {
                font-size: 1rem;
            }

            .custom-button {
                width: 170px;
                height: 48px;
                font-size: 0.9rem;
            }

            .custom-button i {
                font-size: 1rem;
            }
        }
    </style>
</head>

<body>
    <header class="header flex items-center justify-between">
        <div class="flex items-center gap-3">
            <img src="https://ik.imagekit.io/7de5cntkw/1692626704665-photoaidcom-cropped%20(1).jpeg?updatedAt=1738048156918"
                alt="Nimtt Logo" class="w-10 h-10 rounded-full">
            <h1 class="text-2xl md:text-3xl font-bold">Nimtt Student Support</h1>
        </div>
    </header>

    <div class="container mx-auto px-4 py-16 max-w-5xl flex-grow flex items-center justify-center">
        <div class="text-center w-full">
            <div class="mb-6">
                <i class="fas fa-graduation-cap text-5xl text-white"></i>
            </div>
            <h1 class="text-4xl md:text-5xl font-bold mb-6">
                Welcome to Nimtt Student Support
            </h1>
            <p class="text-lg mb-10 text-gray-100 max-w-2xl mx-auto">
                Empowering students with seamless support services. Choose an option below to get started.
            </p>
            <div class="button-grid">
                <button class="custom-button" onclick="window.location.href='/support-department-login'">
                    <i class="fas fa-headset"></i> Support Dept
                </button>
                <button class="custom-button" onclick="window.location.href='/admin-login'">
                    <i class="fas fa-user-shield"></i> Admin
                </button>
            </div>
        </div>
    </div>

    <footer class="footer text-sm text-gray-200">
        <p>© 2025 Nimtt Student Support. All rights reserved.</p>
    </footer>

    <script>
        document.querySelectorAll('.custom-button').forEach(button => {
            button.addEventListener('click', (e) => {
                const rect = button.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;
                button.style.setProperty('--ripple-x', `${x}px`);
                button.style.setProperty('--ripple-y', `${y}px`);
            });
        });
    </script>
</body>

</html>